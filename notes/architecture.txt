Docker containers:
1. frontend, python
2. db, python with sqlite3
3. backend, python with flask

User loggs in with account.
User can add and modiffy trigers for stocks.
User is notified if their stock watch is triggered


requierments.txt will contain all pip package requirements(flask, flask-restful, etc)

docker image will be python.onbuild. onbuild will install all package requirements from the requierments.txt file.
